# Discovered Rules

Hard constraints are recorded only when a human states them as ALWAYS / NEVER rules. The interview chose practices (merge history, walking skeleton, test-after, CI green with no coverage floor, the FRE ship path, and formatter/linter style). The ship process is recorded under Deployment in `team-practices.md`, not restated here as invented ALWAYS lines.

## Mandated

None.

## Forbidden

None.
